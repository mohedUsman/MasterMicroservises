#here we define the production profile for the application and the properties in default is default

#when we run the application with the production profile, this file will be used
spring:
  config:
    activate:
      on-profile: "prod"

build:
  version: "1.0"

accounts:
  message: "Hello from Accounts Service by usman"
  contactDetails:
    email: "xyz@gamil.com"
    name: "xyz"
  onCallSupport:
    -(555) 555-1212
    -(555) 555-2342